# V07-B1 — Brancaforte → Gonza

Status: GREEN / VERIFIED  
Round: R005-V07-GAMEPLAY-PRESENTATION-EXPANSION  
Task: V07-B1  
Recipient: Gonza  
Accepted exact SHA: `9688764c7fb4c61a6b7462c6b34265f328e28a4d`

## Dependency provenance

Mario V07-M2 exact green SHA `a47326093ae004c602e00112ac2ed2226cf738c8` is a real ancestor of this B1 candidate. Brancaforte merged that accepted renderer commit as a second parent before final UI wiring; no renderer code was copied or re-authored by Brancaforte.

The earlier B1 candidates `b851ae8a...` and `18de83af...` are superseded and must not be integrated.

## Files owned/changed by Brancaforte

- `src/game/ui/flow.ts`
- `src/game/ui/AppController.ts`
- `src/styles.css`
- `tests/ui-v07.test.mjs`

Mario-owned renderer code is inherited unchanged from accepted M2.

## Delivered behavior

- `CpuDifficulty` is carried in flow state with Normal default.
- Easy / Normal / Hard selector lives on CPU/opponent select.
- Difficulty persists through stage select, VS, fight, result and rematch.
- Match setup passes the exact selected difficulty to `CpuController`; UI does not decide combat legality.
- Four released fighters remain registry-driven and selectable for player/CPU.
- Fighter cards, selected fighter detail and VS surfaces expose keyed portrait canvas hosts.
- UI imports and calls Mario's `mountFighterPortraits(this.root)` after roster and VS markup creation.
- Registered `portraitKey` selects the renderer; Mario owns all portrait pixels.
- Old character mark remains fallback/accent only and is hidden after successful procedural portrait mount.
- Brancaforte contains no fighter-specific portrait art.
- 5/10 roster-density fixture behavior remains compact/dense.
- Existing touch/keyboard/safe-area behavior is preserved.

## Verification

Repository verification run #1127: SUCCESS.
- coordination contract: success
- full test suite: success
- build/typecheck: success
- B1 tests cover difficulty default/persistence/rematch, portrait mount contract, renderer consumption, ownership boundary and roster density.
- ancestry check: `a4732609...` → `9688764c...` is fast-forward ancestry with no M2 commits missing.

## Known risks

Final integration must preserve this ancestry or reproduce the same accepted deltas without dropping `PortraitRenderer` mounting. No open Brancaforte blocker remains.

## Requested next action

V07-Z0 is now eligible. Gonza should integrate the accepted exact candidate and run final tests/typecheck/build/standalone/parity/integrated smoke per its task.
