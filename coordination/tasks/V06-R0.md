# V06-R0 — Character composition / contracts / validation

Round: R004-V06-CONTENT-EXPANSION  
Owner: Ricardo  
Branch: `round/r004-ricardo`  
Status: VERIFIED  
Depends: V06-N0 VERIFIED  
Auto-chain next: V06-R1. Presentation lanes remain gated by R3 → G1 per CURRENT_ROUND.

## Goal

Remove the remaining two-fighter content assumptions and freeze the typed interface that Juanchi/render/UI will consume, while preserving V0.5 combat behavior.

## Required

Implement C1/content composition from the V0.6 system design:
- `CombatCharacterContent`;
- modules for Camaleoni, Supernariz, Juanchi;
- deeply cloned/frozen combat registry-owned definitions;
- presentation registry metadata;
- visual keys instead of identity fallback contracts;
- validation for duplicate IDs, missing refs, nonfinite values/timelines, cycles, invalid probabilities;
- fourth synthetic package fixture proving registration beyond released roster.

Existing values move without tuning in R0.

## Ownership

Primary:
- `src/game/types.ts`
- `src/game/data/**`
- composition-facing simulation normalization/tests

Do not implement Juanchi gameplay primitives yet.

## Acceptance

- V0.5 deterministic traces/regressions remain green;
- malformed content is rejected explicitly;
- source mutation after registration cannot mutate registry state;
- synthetic fourth package registers without becoming released playable content;
- exported field names/reset semantics are documented in handoff.

## Escalate to user

Only if preserving V0.5 behavior requires changing a frozen V0.6 schema/meaning or a hidden architecture conflict makes the agreed package impossible. Ordinary implementation bugs stay inside R0.

## Handoff

Commit exact SHA + exported interfaces + tests. Green handoff recorded at `coordination/handoffs/V06-R0-ricardo.md`. CURRENT_ROUND unlocks V06-R1 next; presentation lanes remain gated by G1.
