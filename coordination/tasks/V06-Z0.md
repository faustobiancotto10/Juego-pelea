# V06-Z0 — Reproducible V0.6 assembly / tooling

Round: R004-V06-CONTENT-EXPANSION  
Owner: Gonza  
Branch: `round/r004-integration`  
Status: READY  
Depends: V06-N0 VERIFIED  
Auto-chain next: maintain candidate as accepted handoffs arrive; V06-Z1 waits for G1 APPROVE.

## Goal

Prepare a deterministic integration path from the exact V0.5 product base and assemble only explicit green V0.6 handoff SHAs.

## Required

- start from `2d8c41dc994c8cddaa4c9fe2b3ee957698e94f13`;
- recover/preserve deterministic standalone generation;
- fix test discovery so nested `tests/characters/*.test.mjs` cannot be silently omitted;
- pin/record build tooling used for candidate;
- integrate only exact task handoff SHAs;
- never overwrite current main coordination with feature-branch copies;
- maintain source→standalone fingerprint/parity evidence;
- expose one exact candidate SHA for G1.

Z0 may progress in parallel as green handoffs arrive. It does not tune gameplay/render/UI.

## Escalation

Integration conflicts that change semantics, missing accepted input or parity failures are BLOCKERS: record them and tell the user rather than inventing a merge.

## Handoff

Exact assembled candidate SHA + full tests/typecheck/build/standalone evidence unlocks G1 once R3/M2/B1 are present.
