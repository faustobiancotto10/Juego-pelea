# V07-R0 — Schemas + diagnostics baseline

Round: R005-V07-GAMEPLAY-PRESENTATION-EXPANSION  
Owner: Ricardo  
Branch: `round/r005-ricardo`  
Status: WORKING  
Depends: V07-N0 VERIFIED  
Auto-chain next: V07-R1

## Goal

Freeze the minimal typed interfaces V0.7 needs and establish deterministic baselines before gameplay tuning.

## Required

1. Add `CpuDifficulty = 'easy' | 'normal' | 'hard'`.
2. Add `portraitKey` to fighter presentation metadata/validation.
3. Add one bounded move movement definition for committed forward Specials:
   - start/end frame;
   - speed;
   - forward kind;
   - wall stop.
4. Add `forwardBlast` to Ultimate schema/validation with only the fields required by Super Eructo.
5. Preserve all existing V0.6 behavior before R1/R2 data changes.
6. Add deterministic diagnostic harnesses:
   - Lengua repeated-use approach scenarios;
   - CPU difficulty policy traces/corpus;
   - new schema malformed-data cases.

## Ownership

- `src/game/types.ts`
- `src/game/data/**`
- `src/game/simulation/moves.ts`
- schema/fixture tests

Do not implement final El Toro kit or presentation.

## Acceptance

- V0.6 tests remain green before tuning;
- validators reject malformed movement/forwardBlast/portrait data;
- no generic scripting/animation engine;
- exact exported interface names/fields documented in handoff;
- diagnostics produce reproducible values.

## Escalate

Only if the frozen V0.7 architecture cannot be represented with these bounded additions without broader engine change.

## Handoff

Green exact-SHA handoff automatically unlocks V07-R1.
